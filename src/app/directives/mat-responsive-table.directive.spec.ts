import { MatResponsiveTableDirective } from './mat-responsive-table.directive';

describe('MatResponsiveTableDirective', () => {
  it('should create an instance', () => {
    const directive = new MatResponsiveTableDirective();
    expect(directive).toBeTruthy();
  });
});
