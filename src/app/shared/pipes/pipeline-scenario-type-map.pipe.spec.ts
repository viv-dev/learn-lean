import { PipelineScenarioTypeMapPipe } from './pipeline-scenario-type-map.pipe';

describe('PipelineScenarioTypeMapPipe', () => {
  it('create an instance', () => {
    const pipe = new PipelineScenarioTypeMapPipe();
    expect(pipe).toBeTruthy();
  });
});
